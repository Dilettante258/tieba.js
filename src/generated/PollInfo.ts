// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.4
// source: PollInfo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "tbclient";

export interface PollInfo {
  isMulti: number;
  totalNum: string;
  options: PollInfo_PollOption[];
  totalPoll: string;
  title: string;
}

export interface PollInfo_PollOption {
  num: string;
  text: string;
}

function createBasePollInfo(): PollInfo {
  return { isMulti: 0, totalNum: "0", options: [], totalPoll: "0", title: "" };
}

export const PollInfo: MessageFns<PollInfo> = {
  encode(message: PollInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isMulti !== 0) {
      writer.uint32(16).int32(message.isMulti);
    }
    if (message.totalNum !== "0") {
      writer.uint32(24).int64(message.totalNum);
    }
    for (const v of message.options) {
      PollInfo_PollOption.encode(v!, writer.uint32(74).fork()).join();
    }
    if (message.totalPoll !== "0") {
      writer.uint32(88).int64(message.totalPoll);
    }
    if (message.title !== "") {
      writer.uint32(98).string(message.title);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PollInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePollInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isMulti = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalNum = reader.int64().toString();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.options.push(PollInfo_PollOption.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.totalPoll = reader.int64().toString();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.title = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PollInfo {
    return {
      isMulti: isSet(object.isMulti)
        ? globalThis.Number(object.isMulti)
        : isSet(object.is_multi)
        ? globalThis.Number(object.is_multi)
        : 0,
      totalNum: isSet(object.totalNum)
        ? globalThis.String(object.totalNum)
        : isSet(object.total_num)
        ? globalThis.String(object.total_num)
        : "0",
      options: globalThis.Array.isArray(object?.options)
        ? object.options.map((e: any) => PollInfo_PollOption.fromJSON(e))
        : [],
      totalPoll: isSet(object.totalPoll)
        ? globalThis.String(object.totalPoll)
        : isSet(object.total_poll)
        ? globalThis.String(object.total_poll)
        : "0",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
    };
  },

  toJSON(message: PollInfo): unknown {
    const obj: any = {};
    if (message.isMulti !== 0) {
      obj.isMulti = Math.round(message.isMulti);
    }
    if (message.totalNum !== "0") {
      obj.totalNum = message.totalNum;
    }
    if (message.options?.length) {
      obj.options = message.options.map((e) => PollInfo_PollOption.toJSON(e));
    }
    if (message.totalPoll !== "0") {
      obj.totalPoll = message.totalPoll;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    return obj;
  },

  create(base?: DeepPartial<PollInfo>): PollInfo {
    return PollInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PollInfo>): PollInfo {
    const message = createBasePollInfo();
    message.isMulti = object.isMulti ?? 0;
    message.totalNum = object.totalNum ?? "0";
    message.options = object.options?.map((e) => PollInfo_PollOption.fromPartial(e)) || [];
    message.totalPoll = object.totalPoll ?? "0";
    message.title = object.title ?? "";
    return message;
  },
};

function createBasePollInfo_PollOption(): PollInfo_PollOption {
  return { num: "0", text: "" };
}

export const PollInfo_PollOption: MessageFns<PollInfo_PollOption> = {
  encode(message: PollInfo_PollOption, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.num !== "0") {
      writer.uint32(16).int64(message.num);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PollInfo_PollOption {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePollInfo_PollOption();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.num = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PollInfo_PollOption {
    return {
      num: isSet(object.num) ? globalThis.String(object.num) : "0",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
    };
  },

  toJSON(message: PollInfo_PollOption): unknown {
    const obj: any = {};
    if (message.num !== "0") {
      obj.num = message.num;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create(base?: DeepPartial<PollInfo_PollOption>): PollInfo_PollOption {
    return PollInfo_PollOption.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PollInfo_PollOption>): PollInfo_PollOption {
    const message = createBasePollInfo_PollOption();
    message.num = object.num ?? "0";
    message.text = object.text ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
