// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.4
// source: Page.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "tbclient";

export interface Page {
  pageSize: number;
  currentPage: number;
  totalCount: number;
  totalPage: number;
  hasMore: number;
  hasPrev: number;
}

function createBasePage(): Page {
  return { pageSize: 0, currentPage: 0, totalCount: 0, totalPage: 0, hasMore: 0, hasPrev: 0 };
}

export const Page: MessageFns<Page> = {
  encode(message: Page, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pageSize !== 0) {
      writer.uint32(8).int32(message.pageSize);
    }
    if (message.currentPage !== 0) {
      writer.uint32(24).int32(message.currentPage);
    }
    if (message.totalCount !== 0) {
      writer.uint32(32).int32(message.totalCount);
    }
    if (message.totalPage !== 0) {
      writer.uint32(40).int32(message.totalPage);
    }
    if (message.hasMore !== 0) {
      writer.uint32(48).int32(message.hasMore);
    }
    if (message.hasPrev !== 0) {
      writer.uint32(56).int32(message.hasPrev);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Page {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.currentPage = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalCount = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.totalPage = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.hasMore = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.hasPrev = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Page {
    return {
      pageSize: isSet(object.pageSize)
        ? globalThis.Number(object.pageSize)
        : isSet(object.page_size)
        ? globalThis.Number(object.page_size)
        : 0,
      currentPage: isSet(object.currentPage)
        ? globalThis.Number(object.currentPage)
        : isSet(object.current_page)
        ? globalThis.Number(object.current_page)
        : 0,
      totalCount: isSet(object.totalCount)
        ? globalThis.Number(object.totalCount)
        : isSet(object.total_count)
        ? globalThis.Number(object.total_count)
        : 0,
      totalPage: isSet(object.totalPage)
        ? globalThis.Number(object.totalPage)
        : isSet(object.total_page)
        ? globalThis.Number(object.total_page)
        : 0,
      hasMore: isSet(object.hasMore)
        ? globalThis.Number(object.hasMore)
        : isSet(object.has_more)
        ? globalThis.Number(object.has_more)
        : 0,
      hasPrev: isSet(object.hasPrev)
        ? globalThis.Number(object.hasPrev)
        : isSet(object.has_prev)
        ? globalThis.Number(object.has_prev)
        : 0,
    };
  },

  toJSON(message: Page): unknown {
    const obj: any = {};
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.currentPage !== 0) {
      obj.currentPage = Math.round(message.currentPage);
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    if (message.totalPage !== 0) {
      obj.totalPage = Math.round(message.totalPage);
    }
    if (message.hasMore !== 0) {
      obj.hasMore = Math.round(message.hasMore);
    }
    if (message.hasPrev !== 0) {
      obj.hasPrev = Math.round(message.hasPrev);
    }
    return obj;
  },

  create(base?: DeepPartial<Page>): Page {
    return Page.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Page>): Page {
    const message = createBasePage();
    message.pageSize = object.pageSize ?? 0;
    message.currentPage = object.currentPage ?? 0;
    message.totalCount = object.totalCount ?? 0;
    message.totalPage = object.totalPage ?? 0;
    message.hasMore = object.hasMore ?? 0;
    message.hasPrev = object.hasPrev ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
